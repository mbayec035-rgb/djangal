{
  "name": "djangue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev -w @djangue/web",
    "build": "npm run build -w @djangue/web",
    "preview": "npm run preview -w @djangue/web"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
